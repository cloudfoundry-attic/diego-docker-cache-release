---
name: docker

templates:
  ctl.sh: bin/ctl
  cgroups-mount.sh: bin/cgroups-mount

packages:
  - docker-1.6.0

properties:
  docker_registry.address:
    description: "Address of interface on which to serve"
    default: "0.0.0.0:8080"
